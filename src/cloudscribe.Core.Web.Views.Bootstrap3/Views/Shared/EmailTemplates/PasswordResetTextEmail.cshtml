@model cloudscribe.Core.Web.ViewModels.Email.PasswordResetEmailViewModel

@{
    Layout = "_LayoutTextEmailNotification";
    ViewData["Title"] = "Password Reset";
    ViewData["Site"] = Model.Tenant;
}

A password reset has been requested for your account at @Model.Tenant.SiteName. If you did not request a password reset, you can ignore this message, otherwise if you need to reset your password please click this link: @Html.Raw(Model.ResetUrl)

