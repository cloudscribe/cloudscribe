@model cloudscribe.Core.Web.ViewModels.Email.ConfirmEmailAddessViewModel

@{
    Layout = "_LayoutTextEmailNotification";
    ViewData["Title"] = "Email Confirmation Required";
    ViewData["Site"] = Model.Tenant;
}

Please confirm your account by clicking this link: @Html.Raw(Model.ConfirmationUrl). 

If you still have the web page open, you can submit the code:

 @Model.ConfirmationCode
