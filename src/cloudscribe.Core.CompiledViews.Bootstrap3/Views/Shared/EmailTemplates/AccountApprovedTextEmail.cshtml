@model cloudscribe.Core.Web.ViewModels.Email.AccountApprovedEmailViewModel 

@{
    Layout = "_LayoutTextEmailNotification";
    ViewData["Title"] = "Account Approved";
    ViewData["Site"] = Model.Tenant;
}

Your account at @Model.Tenant.SiteName has been approved. 
You may now login by clicking here: @Html.Raw(Model.LoginUrl)
